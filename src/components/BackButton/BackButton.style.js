import styled from 'styled-components';

export const ButtonWrapper = styled.div`
	position: absolute;
	top: 0;
	left: 0%;
	padding: 10px 0 0 0;

	> button {
		color: rgba(0, 0, 0, 0.5);
	}

	> button > svg {
		margin-right: 6px;
	}
`;
